const nomeEntidade = 'lotado_em';
const inscricao = 'inscricao';
const cidade = 'cidade_vinculada';
const diretoria = 'diretoria_vinculada';
const unidade = 'unidade_vinculada';
const aprovadosNaLotacao = 'aprovadosNaLotacao';
const diretoriaVinculada = 'diretoriaVinculada';
const cidadeVinculada = 'cidadeVinculada';

export default {
  // atributos--------------------------------------------------------------
  nomeEntidade,
  inscricao,
  cidade,
  diretoria,
  unidade,
  // relacoes----------------------------------------------------------------
  // deve ser o nome da variavel declarada na entidade, nao o nome da relacao
  aprovadosNaLotacao,
  diretoriaVinculada,
  cidadeVinculada,
};
