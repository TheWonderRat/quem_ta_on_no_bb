const nomeEntidade = 'lista';
const inscricao = 'inscricao';
const posicao = 'posicao';
const tipoLista = 'tipo';
const aprovadoVinculado = 'aprovadoVinculado';
const listaVinculada = 'listaVinculada';

export default {
  // atributos------------------
  nomeEntidade,
  inscricao,
  posicao,
  tipoLista,
  // relacoes--------------------
  // deve ser o nome da variavel declarada na entidade, nao o nome da relacao
  aprovadoVinculado,
  listaVinculada,
};
